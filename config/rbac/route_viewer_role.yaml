# permissions for end users to view routes.
apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: goma-operator
    app.kubernetes.io/managed-by: kustomize
  name: route-viewer-role
rules:
- apiGroups:
  - gateway.jkaninda.dev
  resources:
  - routes
  verbs:
  - get
  - list
  - watch
- apiGroups:
  - gateway.jkaninda.dev
  resources:
  - routes/status
  verbs:
  - get
